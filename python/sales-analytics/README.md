# 销售数据分析报表系统

从 CSV/Excel 加载销售数据，使用 pandas 进行数据清洗、聚合分析，matplotlib + seaborn 生成可视化图表，输出 Markdown + HTML 分析报告。

## 运行

```bash
pip install -r requirements.txt
python analyzer.py
```

生成的报告和图表位于 `./analysis_output/` 目录。
